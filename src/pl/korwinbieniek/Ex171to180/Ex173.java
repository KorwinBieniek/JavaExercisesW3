package pl.korwinbieniek.Ex171to180;

import java.util.Arrays;

public class Ex173 {
    public String arrayOfMedian(int[] arr, int k)
    {
        int[] newarr = new int[arr.length];
        Arrays.sort(arr);

        return Arrays.toString(newarr);
    }
}
